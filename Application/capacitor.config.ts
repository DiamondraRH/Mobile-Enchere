import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EnchereIonic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
